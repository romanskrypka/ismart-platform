# Оптимизация изображений портфолио

## Структура файлов

```
data/img/portfolio/
  └── {project-slug}/
      ├── h/                    # Горизонтальные изображения
      │   ├── raw/             # Оригиналы (без ресайза)
      │   ├── 1280/            # Ресайз до ширины 1280px
      │   └── 640/             # Ресайз до ширины 640px
      └── v/                    # Вертикальные изображения
          ├── raw/             # Оригиналы (без ресайза)
          ├── 1280/            # Ресайз до ширины 1280px
          └── 640/             # Ресайз до ширины 640px
```

## Скрипт ресайза

**Файл:** `scripts/resize_portfolio_images.js`

### Основные настройки

```javascript
const resizeOptions1280 = { width: 1280 };
const resizeOptions640 = { width: 640 };
```

**Важно:** Для всех изображений (горизонтальных и вертикальных) используется ограничение по **ширине**, а не по высоте. Это необходимо для правильной работы `srcset` с дескрипторами ширины (`640w`, `1280w`, `1920w`).

### Запуск скрипта

```bash
# Для всех проектов
node scripts/resize_portfolio_images.js

# Для конкретного проекта
node scripts/resize_portfolio_images.js --project gk1

# Принудительная переконвертация (если файлы уже существуют)
node scripts/resize_portfolio_images.js --project gk1 --force
```

### Параметры качества

- **Формат:** WebP
- **Качество:** 85%
- **Алгоритм:** Sharp (автоматическая оптимизация)

## Структура JSON

**Файл:** `data/json/{lang}/portfolio/{project-slug}.json`

```json
{
  "gallery": [
    {
      "horizontal": [
        {
          "alt": "Описание изображения",
          "cover": true,
          "versions": {
            "raw": "data/img/portfolio/gk1/h/raw/1.webp",
            "1280": "data/img/portfolio/gk1/h/1280/1.webp",
            "640": "data/img/portfolio/gk1/h/640/1.webp"
          }
        }
      ],
      "vertical": [
        {
          "alt": "Описание изображения",
          "cover": true,
          "versions": {
            "raw": "data/img/portfolio/gk1/v/raw/1.webp",
            "1280": "data/img/portfolio/gk1/v/1280/1.webp",
            "640": "data/img/portfolio/gk1/v/640/1.webp"
          }
        }
      ]
    }
  ]
}
```

## Компонент Picture

**Файл:** `templates/components/picture.twig`

### Параметры

- `image` (обязательный) - объект изображения с versions или src
- `alt` - альтернативный текст
- `class` - CSS классы для img
- `sizes_preset` - пресет размеров: "full", "half", "half-mobile", "quarter"
- `sizes` - кастомная строка sizes (переопределяет preset)
- `loading` - "lazy" или "eager" (по умолчанию "lazy")
- `itemprop` - Schema.org атрибут

### Sizes пресеты

```twig
'full': '(max-width: 991px) 100vw, (max-width: 1199px) 1200px, 1400px'
'half': '(max-width: 991px) 100vw, (max-width: 1199px) 600px, 700px'
'half-mobile': '(max-width: 639px) 320px, 640px'
'quarter': '(max-width: 991px) 25vw, (max-width: 1199px) 280px, 320px'
```

### Логика пресетов

#### full (100% контейнера на десктопе)
- **Использование:** Полноэкранные изображения, intro слайдеры
- **Контейнер:** max 1400px (на экранах ≥1441px)
- **Для 1x DPR:** загрузится 1280px или raw
- **Для 2x DPR:** загрузится raw (~1920px)

#### half (50% контейнера на десктопе)
- **Использование:** Cover изображения галереи (horizontal)
- **Контейнер:** ~600-700px (50% от 1200-1400px)
- **Для 1x DPR:** загрузится 640px
- **Для 2x DPR:** загрузится 1280px ✅

#### half-mobile (для вертикальных cover)
- **Использование:** Cover изображения галереи (vertical)
- **На мобильных ≤639px:** sizes=320px → для 2x загрузится 640px
- **На планшетах >639px:** sizes=640px → для 2x загрузится 1280px ✅

**Почему не 100vw?**
При ширине 690px и sizes=100vw браузер для 2x DPR нужно 1380px, а версия 1280px недостаточна. Указывая sizes=640px, браузер выбирает 1280px версию (ближайшую большую), которой достаточно.

#### quarter (25% контейнера на десктопе)
- **Использование:** Thumbnails галереи
- **Контейнер:** ~280-320px (25% от 1200-1400px)
- **Для 1x DPR:** загрузится 640px
- **Для 2x DPR:** загрузится 640px ✅ (320px × 2 = 640px)

### Пример использования

```twig
{% include 'components/picture.twig' with {
  image: item,
  alt: item.alt|default('Gallery image'),
  class: 'gallery__cover img-responsive',
  sizes_preset: 'half',
  loading: 'lazy'
} %}
```

## Генерируемый HTML

```html
<picture>
  <source 
    srcset="
      http://localhost:8888/data/img/portfolio/gk1/h/640/1.webp 640w, 
      http://localhost:8888/data/img/portfolio/gk1/h/1280/1.webp 1280w, 
      http://localhost:8888/data/img/portfolio/gk1/h/raw/1.webp 1920w
    " 
    sizes="(max-width: 991px) 100vw, (max-width: 1199px) 600px, 700px" 
    type="image/webp">
  <img 
    src="http://localhost:8888/data/img/portfolio/gk1/h/1280/1.webp" 
    alt="MR Group, ЖК-1" 
    loading="lazy" 
    decoding="async"
    class="gallery__cover img-responsive">
</picture>
```

## Breakpoints проекта

**Файл:** `assets/css/base/mq.css`

```css
@custom-media --xs (max-width: 539px);
@custom-media --xm (min-width: 540px);
@custom-media --sm (min-width: 768px);
@custom-media --md (min-width: 992px);
@custom-media --lg (min-width: 1200px);
@custom-media --xl (min-width: 1441px);
@custom-media --xxl (min-width: 1601px);
```

## Размеры контейнеров

**Файл:** `assets/css/base/grid.css`

```css
.container {
  /* max-width: 1200px на --lg (≥1200px) */
  /* max-width: 1400px на --xl (≥1441px) */
}

.gallery .section__item.cover-wrap {
  /* width: calc(50% - 1rem) на --lg */
  /* = ~600px (lg) или ~700px (xl) */
}

.gallery__subitem.thumb-wrap {
  /* width: calc(25% - 2rem) на --lg */
  /* = ~280px (lg) или ~330px (xl) */
}
```

## Ориентация изображений

### Horizontal (h/)
- **Показываются:** На десктопе (≥992px)
- **Скрываются:** На мобильных (<992px)
- **CSS:** `[data-orientation="horizontal"]`

### Vertical (v/)
- **Показываются:** На мобильных (<992px)
- **Скрываются:** На десктопе (≥992px)
- **CSS:** `[data-orientation="vertical"]`

### Universal
- **Показываются:** Везде
- **Использование:** Когда нет пары horizontal/vertical
- **CSS:** `[data-orientation="universal"]`

## Оптимизация для DPR (Device Pixel Ratio)

Браузер автоматически выбирает версию изображения на основе:
1. **sizes** - объявленный размер контейнера
2. **DPR** - плотность пикселей устройства (1x, 2x, 3x)
3. **srcset** - доступные версии с дескрипторами ширины

### Пример расчета

**Thumbnail на Retina дисплее:**
- Контейнер: 320px
- DPR: 2x
- Требуется: 320px × 2 = 640px
- sizes: `280px` или `320px`
- Браузер выбирает: **640px версию** ✅

**Cover на iPad (768px):**
- Контейнер: 100vw = 768px
- DPR: 2x
- Требуется: 768px × 2 = 1536px
- sizes: `640px` (из half-mobile)
- Браузер выбирает: **1280px версию** ✅

## Миграция старых проектов

### Скрипт обновления JSON

**Файл:** `scripts/update_portfolio_json.py`

```bash
# Обновить все JSON файлы
python3 scripts/update_portfolio_json.py
```

Скрипт автоматически преобразует старую структуру:
```json
"src": "data/img/portfolio/saan/horizontal/raw/1.webp"
```

В новую:
```json
"versions": {
  "raw": "data/img/portfolio/saan/h/raw/1.webp",
  "1280": "data/img/portfolio/saan/h/1280/1.webp",
  "640": "data/img/portfolio/saan/h/640/1.webp"
}
```

## Проверка результатов

### DevTools → Network

1. Открыть страницу проекта (например, `/portfolio/gk1/`)
2. DevTools → Network → Img
3. Проверить загружаемые версии:
   - **Thumbnails:** должны загружать `640/` версии
   - **Covers:** должны загружать `1280/` версии (на 2x)
   - **Raw:** загружается только при очень больших экранах

### Симуляция разных DPR

DevTools → Settings (⚙️) → Devices → Add custom device:
- DPR: 1, 2, 3
- Viewport: 375px, 768px, 1440px

### Ожидаемые размеры файлов

Для оптимизации загрузки:
- **640px версия:** ~50-150 KB
- **1280px версия:** ~150-400 KB
- **raw версия:** ~300-800 KB

## Troubleshooting

### Проблема: Загружается слишком большая версия

**Причина:** sizes завышен для данного контейнера

**Решение:** Уменьшить значение в sizes пресете

### Проблема: Изображение размытое

**Причина:** Для DPR 2x загружается 1x версия (sizes занижен)

**Решение:** Увеличить значение в sizes пресете или проверить srcset

### Проблема: Вертикальное изображение не покрывает контейнер

**Причина:** При определенной ширине браузера (например, 690px) выбирается недостаточная версия

**Решение:** Использовать пресет с фиксированными размерами вместо vw, например `half-mobile: '(max-width: 639px) 320px, 640px'`

## История изменений

### 2025-11-12
- ✅ Изменен ресайз вертикальных изображений с `height` на `width`
- ✅ Добавлен компонент `picture.twig` с пресетами sizes
- ✅ Внедрены оптимизированные sizes для thumbnails (quarter)
- ✅ Добавлен пресет `half-mobile` для вертикальных cover
- ✅ Пересозданы все изображения проекта gk1

### Результат
- 📉 Уменьшена загрузка thumbnails с 1280px до 640px (~60% экономия)
- 📉 Оптимизирована загрузка вертикальных изображений
- ✅ Правильная работа на всех разрешениях и DPR








