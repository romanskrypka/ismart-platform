
# Опросник для полной спецификации проекта под LLM (Расширенная версия)

**Цель:** Этот документ предназначен для сбора всей необходимой информации, чтобы LLM-ассистент (Cursor) мог самостоятельно сгенерировать полную проектную документацию и реализовать проект с нуля, следуя методологиям PCAM, GRACE и FLEX.

**Инструкция:** Пожалуйста, ответьте на вопросы в каждом блоке как можно подробнее. Для сложных вопросов, требующих анализа, предоставлены пошаговые алгоритмы. Ваши ответы станут "источником правды" для ИИ.

---

## Блок 1: Видение и цели проекта (Project Vision & Goals)

*Этот блок определяет высокоуровневую стратегию проекта. Ответы из этого блока напрямую используются для формирования `DEVELOPMENT_PLAN.md` (раздел "Видение проекта") и `.cursorrules` (глобальная цель).*

**1.1. Название проекта:**
*Как будет называться ваш проект?*

> **Развернутый пример:**
> - **Ответ:** `iSmart Platform`
> - **Комментарий:** Название должно быть коротким, запоминающимся и отражать суть. `iSmart` намекает на интеллектуальные (AI) возможности.

---

**1.2. Цель проекта (одним предложением):**
*Опишите суть проекта в одном предложении, отвечая на вопросы "Что это?" и "Для кого это?".*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм Elevator Pitch**, чтобы сформулировать цель четко и лаконично.*
> 
> 1.  **Для (целевая аудитория):** `Операторы склада и менеджеры по логистике`
> 2.  **Которые (проблема/потребность):** `сталкиваются с ошибками и задержками из-за ручного учёта товаров`
> 3.  **Наш проект (название):** `iSmart Platform`
> 4.  **Это (категория продукта):** `веб-платформа для автоматизации складских операций`
> 5.  **Которая (ключевое преимущество):** `использует ИИ-агентов для оптимизации размещения товаров и контроля остатков в реальном времени`
> 6.  **В отличие от (альтернативы):** `стандартных WMS-систем и Excel-таблиц`
> 7.  **Наше решение (ключевое отличие):** `предоставляет пошаговые инструкции операторам и проактивно находит узкие места в логистике.`

> **Развернутый пример ответа:**
> `Веб-платформа iSmart Platform для операторов склада и менеджеров по логистике, которая, в отличие от Excel и стандартных WMS, использует ИИ-агентов для оптимизации размещения товаров и контроля остатков в реальном времени, помогая сократить ошибки и ускорить складские операции.`

---

**1.3. Проблема, которую решает проект:**
*Какую конкретную "боль" или неэффективность вы устраняете? Почему этот проект важен?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "5 Почему" (5 Whys)**, чтобы докопаться до корневой проблемы.*
> 
> 1.  **Проблема:** Мы теряем клиентов.
> 2.  **Почему?** Потому что мы задерживаем отгрузки.
> 3.  **Почему?** Потому что сборка заказов занимает много времени.
> 4.  **Почему?** Потому что операторы долго ищут нужный товар на складе.
> 5.  **Почему?** Потому что в нашей системе учета (Excel) нет точной информации о местонахождении товара, и операторы ищут его вручную.
> **Вывод (корневая проблема):** Отсутствие системы адресного хранения и учёта в реальном времени приводит к медленной сборке заказов, задержкам отгрузки и потере клиентов.

> **Развернутый пример ответа:**
> `Корневая проблема заключается в отсутствии системы адресного хранения. Операторы тратят до 40% времени на ручной поиск ячеек для размещения и сборки товара, сверяясь с неактуальными Excel-таблицами. Это приводит к систематическим ошибкам в учёте остатков, замедляет скорость сборки заказов на 30%, вызывает задержки отгрузок и, как следствие, приводит к потере клиентов (отток ~15% в год).`

---

**1.4. Ключевые бизнес-цели и KPI:**
*Как вы будете измерять успех проекта? Укажите 2-3 измеримых показателя.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм SMART** для постановки целей.*
> - **S (Specific):** Конкретная. *Что именно мы хотим улучшить?* (Скорость сборки заказа).
> - **M (Measurable):** Измеримая. *Как мы измерим улучшение?* (В минутах на сборку одного заказа).
> - **A (Achievable):** Достижимая. *Реально ли этого достичь?* (Да, за счет автоматизации поиска).
> - **R (Relevant):** Релевантная. *Связана ли эта цель с проблемой?* (Да, это напрямую решает проблему медленной сборки).
> - **T (Time-bound):** Ограниченная по времени. *К какому сроку?* (За 3 месяца после внедрения MVP).

> **Развернутый пример ответа:**
> `1. **Сократить среднее время сборки одного заказа на 30%** (с 25 до 17.5 минут) в течение 3 месяцев после запуска MVP.`
> `2. **Снизить количество расхождений при инвентаризации до 0.5%** (с текущих 5%) по итогам первого квартала использования системы.`
> `3. **Увеличить пропускную способность склада (количество отгрузок в день) на 20%** в течение 6 месяцев.`

---

**1.5. Границы MVP (Minimum Viable Product):**
*Что абсолютно необходимо для запуска первой версии? А что точно НЕ входит в неё?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм MoSCoW** для приоритизации функциональности.*
> - **M (Must have):** Должно быть. Без этого система нежизнеспособна. (например, `автоматический подбор ячейки`).
> - **S (Should have):** Следует сделать. Важно, но не критично для запуска. (например, `генерация PDF-отчёта`).
> - **C (Could have):** Могло бы быть. Желательно, но можно отложить. (например, `мобильное приложение для оператора`).
> - **W (Won't have):** Не будет в этот раз. То, что точно не войдет в релиз. (например, `ИИ-прогнозирование спроса`).

> **Развернутый пример ответа:**
> `**MVP ВКЛЮЧАЕТ (Must have):**`
> `- Регистрация и аутентификация пользователей (Оператор, Менеджер).`
> `- Функционал создания, редактирования и просмотра сущностей: Товар, Ячейка, Склад.`
> `- Основной бизнес-сценарий: автоматический подбор оптимальной ячейки при приёмке товара.`
> `- Обновление остатков в реальном времени.`
> `- Простейший веб-интерфейс для просмотра задач на размещение.`
> 
> `**MVP НЕ ВКЛЮЧАЕТ (Should, Could, Won't):**`
> `- Генерация отчётов (Should have).`
> `- Мобильное приложение (Could have).`
> `- Интеграция с 1С (Won't have).`
> `- ИИ-агент для прогнозирования спроса (Won't have).`

---

## Блок 2: Бизнес-логика и функциональность (Business Logic & Functionality)

*Этот блок детализирует, ЧТО система должна делать. Ответы станут основой для моделей данных, бизнес-сценариев в `DEVELOPMENT_PLAN.md` и логики в коде.*

**2.1. Основные бизнес-сценарии (User Stories):**
*Опишите 3-5 ключевых сценариев использования в формате: "[Роль] → [Что делает] → [Какой результат получает]".*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм User Story Mapping (упрощенный)**, чтобы визуализировать путь пользователя.*
> 
> 1.  **Определите "эпики" (крупные шаги):** `Управление товарами`, `Приёмка товара`, `Сборка заказа`, `Аналитика`.
> 2.  **Для каждого эпика напишите User Stories:**
>     - **Эпик "Приёмка товара":**
>         - *Как Оператор склада, я хочу сканировать штрих-код товара, чтобы быстро его идентифицировать.*
>         - *Как Оператор склада, я хочу, чтобы система предложила мне лучшую ячейку для размещения, чтобы не тратить время на поиск.*
>         - *Как Оператор склада, я хочу подтвердить размещение, чтобы система обновила остатки.*
>     - **Эпик "Аналитика":**
>         - *Как Менеджер, я хочу видеть дашборд с остатками в реальном времени, чтобы контролировать ситуацию на складе.*
>         - *Как Менеджер, я хочу генерировать отчёт по оборачиваемости, чтобы выявить неликвидные товары.*
> 3.  **Выберите 3-5 самых важных историй для MVP.**

> **Развернутый пример ответа:**
> `1. **Оператор склада** → Сканирует штрих-код нового товара и вводит его количество → Система предлагает ему оптимальную ячейку для размещения (например, "A-1-05") и создаёт задачу на размещение.`
> `2. **Оператор склада** → Подтверждает размещение товара в ячейке (например, сканированием штрих-кода ячейки) → Система обновляет остатки и закрывает задачу.`
> `3. **Менеджер** → Заходит на страницу "Отчёты" и выбирает "Отчёт по оборачиваемости за период" → Система генерирует и отображает на экране отчёт с указанием, какие товары продаются быстро, а какие лежат без движения.`
> `4. **Система (автоматически)** → Обнаруживает, что остаток товара "Молоток" стал ниже порогового значения (например, 10 штук) → Автоматически создаёт черновик заявки на пополнение и отправляет уведомление Менеджеру по закупкам.`

---

**2.2. Ключевые сущности системы:**
*Перечислите основные "объекты", с которыми работает система. Это будущие модели в Django.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Выделение существительных"** из ваших User Stories и описания проблемы.*
> 
> 1.  **Проанализируйте текст:** "**Оператор склада** сканирует **штрих-код** **товара** и вводит его **количество**. **Система** предлагает ему оптимальную **ячейку** для размещения и создаёт **задачу** на размещение. **Менеджер** запрашивает **отчёт** по оборачиваемости **товаров** за **период**. **Система** обнаруживает, что **остаток** **товара** ниже порогового **значения** и создаёт **заявку на пополнение** для **поставщика**."
> 2.  **Выпишите все существительные:** Оператор, склад, штрих-код, товар, количество, система, ячейка, задача, менеджер, отчёт, период, остаток, значение, заявка, поставщик.
> 3.  **Сгруппируйте и отфильтруйте:**
>     - `Пользователь` (Оператор, Менеджер)
>     - `Товар` (штрих-код, количество, остаток - это его атрибуты)
>     - `Склад`, `Ячейка`
>     - `Задача на размещение`
>     - `Заявка на пополнение`
>     - `Поставщик`
>     - `Отчёт` (это не сущность, а результат действия)
>     - `Система` (это не сущность, а сама программа)

> **Развернутый пример ответа:**
> `- **Пользователь (User):** Сотрудники, работающие с системой (операторы, менеджеры).`
> `- **Товар (Product):** Единица хранения на складе (артикул, название, вес, габариты).`
> `- **Склад (Warehouse):** Физическое здание склада.`
> `- **Ячейка (StorageCell):** Место хранения на складе (адрес, максимальный объём).`
> `- **Партия товара (StockItem):** Конкретная партия товара в конкретной ячейке с количеством.`
> `- **Задача (Task):** Задание для оператора (например, "разместить товар X в ячейке Y").`
> `- **Заявка на пополнение (ReplenishmentOrder):** Документ для заказа товара у поставщика.`
> `- **Поставщик (Supplier):** Компания, поставляющая товары.`

---

**2.3. Бизнес-правила и ограничения:**
*Есть ли какие-то специфические формулы, условия или ограничения, которые система должна соблюдать?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Event Storming (lite)"** — мозговой штурм по событиям.*
> 
> 1.  **Возьмите ключевой процесс:** `Приёмка товара`.
> 2.  **Задайте вопросы о каждом шаге:**
>     - *"Когда оператор хочет разместить товар, что мы должны проверить?"* → `Нужно проверить, что объём товара не превышает свободный объём ячейки.` (Правило 1)
>     - *"А есть ли какие-то особые условия для товаров?"* → `Да, хрупкие товары нельзя класть на верхние полки.` (Правило 2)
>     - *"Что произойдет, если на складе нет места?"* → `Система должна запретить приёмку и уведомить менеджера.` (Правило 3)
>     - *"Может ли оператор выбрать ячейку сам?"* → `Может, но только если система не нашла подходящих вариантов, и у него есть на это права.` (Правило 4)

> **Развернутый пример ответа:**
> `- **Правило валидации размещения:** Объём `Партии товара` не может превышать `свободный объём` `Ячейки`.`
> `- **Правило совместимости:** Товары категории "Хрупкое" могут размещаться только на `Ячейках` с атрибутом `ярус=1`. Товары категории "Химия" не могут храниться рядом с "Продуктами питания".`
> `- **Правило уникальности:** Не может быть двух `Товаров` с одинаковым `артикулом` (SKU).`
> `- **Правило пополнения:** `Заявка на пополнение` не может быть создана, если уже существует другая `Заявка` на этот же `Товар` в статусе "Утверждена" или "В работе".`

---

**2.4. Жизненные циклы (Workflows):**
*Опишите статусы и переходы для 1-2 ключевых сущностей. Это основа для модуля "Процессы" в АРМ.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм построения конечного автомата (FSM)**.*
> 
> 1.  **Выберите сущность:** `Заявка на пополнение`.
> 2.  **Определите все возможные состояния (статусы), в которых она может находиться:** `Черновик`, `На согласовании`, `Утверждена`, `Отклонена`, `В работе` (заказана у поставщика), `Выполнена` (товар получен), `Архивирована`.
> 3.  **Определите события (триггеры), которые переводят сущность из одного статуса в другой:** `submit`, `approve`, `reject`, `order`, `receive`, `archive`.
> 4.  **Нарисуйте диаграмму состояний (или просто опишите переходы):**

> **Развернутый пример ответа (для сущности "Заявка на пополнение"):**
> `- **Статусы:**`
>   `- `Draft` (Черновик)
>   `- `PendingApproval` (На согласовании)
>   `- `Approved` (Утверждена)
>   `- `Rejected` (Отклонена)
>   `- `InProgress` (В работе)
>   `- `Completed` (Выполнена)
>   `- `Archived` (Архивирована)
> `- **Переходы (события):**`
>   `- `submit()`: `Draft` → `PendingApproval` (совершает Менеджер)
>   `- `approve()`: `PendingApproval` → `Approved` (совершает Руководитель отдела закупок)
>   `- `reject()`: `PendingApproval` → `Rejected` (совершает Руководитель отдела закупок)
>   `- `order()`: `Approved` → `InProgress` (совершает Менеджер после отправки заказа поставщику)
>   `- `complete()`: `InProgress` → `Completed` (совершает Оператор склада при получении товара)
>   `- `archive()`: `Completed` / `Rejected` → `Archived` (совершается автоматически через 90 дней)`

---

## Блок 3: Пользователи и роли (Users & Roles)

*Этот блок определяет, КТО будет использовать систему и какие у них будут права. Эта информация используется для настройки аутентификации, авторизации и для заполнения вашего личного профиля `USER_PROFILE.xml`.*

**3.1. Роли пользователей в системе:**
*Перечислите все роли, которые будут в системе.*

> **Развернутый пример ответа:**
> `- **`admin`** (Администратор системы)
> `- **`purchasing_manager`** (Менеджер по закупкам)
> `- **`warehouse_manager`** (Менеджер склада)
> `- **`warehouse_operator`** (Оператор склада)
> `- **`guest`** (Гость, только просмотр)`

---

**3.2. Права доступа (матрица доступа):**
*Для каждой роли кратко опишите, что она может делать, а что нет.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм построения RBAC-матрицы (Role-Based Access Control)**. Создайте таблицу, где строки — это сущности/действия, а столбцы — роли.*

> **Развернутый пример ответа:**
> `| Сущность/Действие | Администратор | Менеджер склада | Менеджер по закупкам | Оператор склада |`
> `|---|---|---|---|---|
> `| **Пользователи** | CRUD | - | - | - |
> `| **Склад/Ячейки** | CRUD | R | - | - |
> `| **Товары** | CRUD | R | R | R |
> `| **Заявки на пополнение** | R | R | CRUD | - |
> `| **Задачи на размещение** | R | CRUD | - | R (только свои) |
> `| **Отчёты** | R | R | R | - |
> `* (C - Create, R - Read, U - Update, D - Delete)`

---

**3.3. Ваш профиль как оператора ИИ (для USER_PROFILE.xml):**
*Эти вопросы касаются лично вас и вашего стиля работы с ИИ. Здесь нет правильных или неправильных ответов, будьте честны.*

> **Развернутый пример ответа:**
> `- **Ваша основная роль в проекте?** `Архитектор и тимлид.`
> `- **Предпочитаемый стиль ответов ИИ?** `Развёрнутый с объяснениями, чтобы я мог понять логику и принять решение.`
> `- **Предпочитаемый стиль взаимодействия?** `Сначала задавать уточняющие вопросы, предлагать несколько вариантов решения, если они есть.`
> `- **Предпочитаете микросервисы или монолит?** `Монолит для MVP, с четким разделением на модули (приложения Django) для будущего возможного разделения на сервисы.`
> `- **Насколько подробно нужно комментировать код?** `Подробно, особенно для сложной бизнес-логики. Использовать GRACE-разметку обязательно.`
> `- **Какие задачи вы готовы делегировать ИИ?** `Генерация кода по контракту, написание тестов, рефакторинг, написание документации, первичный анализ ошибок по логам.`
> `- **Ваше отношение к техническому долгу?** `Допустим в MVP в виде `TODO`-комментариев, но должен быть задокументирован и включен в бэклог следующей версии.`
> `- **Предпочитаете open-source решения или коммерческие?** `Предпочитаю проверенные open-source решения, но готов использовать коммерческие, если они дают значительное преимущество.`

---

## Блок 4: Архитектура и Технический Стек (Architecture & Tech Stack)

*Этот блок фиксирует технические решения и ограничения. Ответы используются для `DEVELOPMENT_PLAN.md` (раздел "Технический стек" и "Решения и ограничения") и для `USER_PROFILE.xml`.*

**4.1. Основной стек:**
*Укажите основные технологии, которые вы планируете использовать.*

> **Развернутый пример ответа:**
> `- **Язык программирования:** Python 3.11+`
> `- **Основной фреймворк:** Django 5.x`
> `- **База данных (для разработки):** SQLite`
> `- **База данных (для продакшена):** PostgreSQL 15+`
> `- **Фронтенд-подход:** Django Templates + HTMX + TailwindCSS (для быстрой разработки динамичных интерфейсов без тяжелого JS-фреймворка).`

---

**4.2. Архитектурный стиль:**
*Какой подход к архитектуре вы предпочитаете?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Матрица решений" (Decision Matrix)**, чтобы объективно выбрать архитектурный стиль.*
> 
> 1.  **Определите критерии:** `Скорость разработки`, `Стоимость поддержки`, `Масштабируемость`, `Надёжность`, `Порог входа для новых разработчиков`.
> 2.  **Присвойте веса каждому критерию (от 1 до 5):**
>     - `Скорость разработки`: 5 (самое важное для MVP)
>     - `Стоимость поддержки`: 4
>     - `Порог входа`: 4
>     - `Масштабируемость`: 3 (важно в будущем, но не сейчас)
>     - `Надёжность`: 3
> 3.  **Оцените каждый вариант (Монолит, Микросервисы) по каждому критерию (от 1 до 10):**
> 
> | Критерий (Вес) | Монолит (Оценка) | Монолит (Итог) | Микросервисы (Оценка) | Микросервисы (Итог) |
> |---|---|---|---|---|
> | Скорость разработки (5) | 9 | 45 | 5 | 25 |
> | Стоимость поддержки (4) | 8 | 32 | 4 | 16 |
> | Порог входа (4) | 9 | 36 | 5 | 20 |
> | Масштабируемость (3) | 6 | 18 | 9 | 27 |
> | Надёжность (3) | 7 | 21 | 8 | 24 |
> | **Всего** | | **152** | | **112** |
> 
> **Вывод:** Для текущего проекта (MVP) монолитная архитектура является более предпочтительной.

> **Развернутый пример ответа:**
> `Для MVP выбран **Монолит** с четким разделением на модули (Django Apps). Это решение обеспечивает максимальную скорость разработки и низкий порог входа для команды. Вопросы масштабируемости будут решаться по мере роста проекта; модульная структура позволит в будущем относительно безболезненно вынести отдельные приложения в микросервисы.`

---

**4.3. Ключевые библиотеки и фреймворки:**
*Есть ли какие-то конкретные библиотеки, которые вы хотите использовать?*

> **Развернутый пример ответа:**
> `- **API:** `django-rest-framework` для создания REST API, `drf-spectacular` для автоматической генерации OpenAPI (Swagger) документации.`
> `- **Фоновые задачи:** `celery` для асинхронных операций, `redis` в качестве брокера сообщений.`
> `- **Процессы:** `django-fsm` для реализации конечных автоматов (жизненных циклов документов).`
> `- **Иерархии:** `django-mptt` для древовидных структур (например, категории товаров, оргструктура).`
> `- **ИИ:** `langchain` для работы с LLM, `pgvector` для векторного поиска в PostgreSQL.`

---

**4.4. Технические ограничения:**
*Есть ли какие-то технологии, которые вы НЕ хотите или НЕ можете использовать?*

> **Развернутый пример ответа:**
> `- **Контейнеризация:** На этапе MVP **не использовать Docker** для упрощения процесса развертывания. Деплой будет осуществляться через systemd и gunicorn.`
> `- **Облачный провайдер:** Использовать **только Yandex Cloud**, так как на него получены гранты.`
> `- **CI/CD:** На этапе MVP **не настраивать**. Сборка и деплой будут производиться вручную по инструкции.`
> `- **Базы данных:** Не использовать NoSQL базы данных (например, MongoDB), так как данные хорошо структурированы.`

---

## Блок 5: Данные и Модели (Data & Models)

*Этот блок — глубокое погружение в структуру данных. На основе этих ответов ИИ будет генерировать модели Django. Для каждой сущности из пункта 2.2, опишите её структуру.*

**5.1. Детализация сущностей:**
*Для каждой сущности, перечислите её поля, их типы и свойства.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм проектирования моделей** для каждой сущности.*
> 
> 1.  **Название сущности:** `Товар (Product)`
> 2.  **Идентификация:** Как мы уникально определяем товар? → `Артикул (SKU)`. Это будет `unique=True`.
> 3.  **Основные атрибуты:** Что нам нужно знать о товаре? → `Название`, `Описание`, `Вес`, `Габариты`.
> 4.  **Системные поля:** Нужны ли нам временные метки? → Да, `Дата создания` и `Дата обновления`. Это будут `auto_now_add=True` и `auto_now=True`.
> 5.  **Типы данных:** Определите тип для каждого поля (строка, число, дата, JSON и т.д.).
> 6.  **Ограничения:** Какое поле обязательное (`blank=False, null=False`), а какое нет? Есть ли значения по умолчанию (`default=...`)?

> **Развернутый пример ответа:**
> `**Сущность "Товар" (`Product`):**`
> `- **`name`** (CharField, max_length=255, `db_index=True`) - Название товара. Обязательное.`
> `- **`sku`** (CharField, max_length=100, `unique=True`) - Артикул. Обязательное.`
> `- **`description`** (TextField, `blank=True`) - Описание товара. Необязательное.`
> `- **`category`** (ForeignKey к `ProductCategory`, `on_delete=models.PROTECT`) - Категория товара. Обязательное.`
> `- **`weight`** (DecimalField, max_digits=10, decimal_places=3) - Вес в килограммах. Обязательное.`
> `- **`dimensions`** (JSONField, `default=dict`) - Габариты (длина, ширина, высота в метрах).`
> `- **`created_at`** (DateTimeField, `auto_now_add=True`) - Дата создания.`
> `- **`updated_at`** (DateTimeField, `auto_now=True`) - Дата последнего обновления.`

---

**5.2. Связи между сущностями:**
*Опишите, как сущности связаны друг с другом.*

> #### 🤔 Как ответить на этот вопрос?
> *Мысленно нарисуйте **ER-диаграмму (Entity-Relationship Diagram)**.*
> 
> 1.  **Возьмите две сущности:** `Заказ (Order)` и `Товар (Product)`.
> 2.  **Задайте вопросы в обе стороны:**
>     - *"Может ли в одном `Заказе` быть много `Товаров`?"* → Да.
>     - *"Может ли один и тот же `Товар` быть во многих `Заказах`?"* → Да.
> 3.  **Определите тип связи:** Если на оба вопроса ответ "Да", это связь **Многие-ко-Многим (ManyToMany)**. В Django это потребует создания промежуточной модели (например, `OrderItem`).
> 4.  **Повторите для других пар:**
>     - `Ячейка` и `Склад`: В одном `Складе` много `Ячеек`, но `Ячейка` принадлежит только одному `Складу` → **Один-ко-Многим (ForeignKey)** со стороны `Ячейки`.
>     - `Пользователь` и `Профиль`: У одного `Пользователя` только один `Профиль` → **Один-к-Одному (OneToOneField)**.

> **Развернутый пример ответа:**
> `- **`Заказ` и `Товар` (ManyToMany):** Связь реализуется через промежуточную модель `OrderItem`, которая хранит `ForeignKey` на `Заказ`, `ForeignKey` на `Товар`, а также дополнительные поля `quantity` (количество) и `price_per_item` (цена за единицу на момент заказа).`
> `- **`Ячейка` и `Склад` (ForeignKey):** Модель `StorageCell` имеет поле `warehouse = models.ForeignKey(Warehouse, on_delete=models.CASCADE)`, так как ячейка не может существовать без склада.`
> `- **`Товар` и `Категория` (ForeignKey):** Модель `Product` имеет поле `category = models.ForeignKey(ProductCategory, on_delete=models.PROTECT)`, чтобы предотвратить удаление категории, если в ней есть товары.`
> `- **`Пользователь` и `Профиль` (OneToOneField):** Модель `UserProfile` имеет поле `user = models.OneToOneField(User, on_delete=models.CASCADE)` для хранения дополнительной информации о пользователе.`

---

## Блок 6: API и Интеграции (API & Integrations)

*Этот блок описывает, как ваша система будет взаимодействовать с внешним миром и другими сервисами.*

**6.1. Внутренний API (если планируется):**
*Будет ли у проекта REST API для фронтенда или мобильного приложения? Если да, опишите 2-3 ключевых эндпоинта.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм проектирования эндпоинтов** на основе CRUD-операций и бизнес-сценариев.*
> 
> 1.  **Выберите сущность:** `Товар (Product)`.
> 2.  **Определите стандартные CRUD-эндпоинты:**
>     - `GET /api/v1/products/` (List) - Получить список товаров.
>     - `POST /api/v1/products/` (Create) - Создать новый товар.
>     - `GET /api/v1/products/{id}/` (Retrieve) - Получить один товар.
>     - `PUT/PATCH /api/v1/products/{id}/` (Update) - Обновить товар.
>     - `DELETE /api/v1/products/{id}/` (Delete) - Удалить товар.
> 3.  **Определите кастомные эндпоинты (actions) на основе бизнес-логики:**
>     - *"Нужно ли нам получать аналитику по товару?"* → Да. Создадим эндпоинт `GET /api/v1/products/{id}/analytics/`.
>     - *"Нужно ли запускать какой-то процесс для товара?"* → Нет.

> **Развернутый пример ответа:**
> `Да, планируется REST API для будущего мобильного приложения.`
> `- **`GET /api/v1/products/`**: Получить список товаров с возможностью фильтрации по категории и поиском по названию/артикулу.`
> `- **`POST /api/v1/orders/`**: Создать новый заказ. Принимает список `OrderItem` с ID товаров и их количеством.`
> `- **`GET /api/v1/orders/{id}/`**: Получить детальную информацию о конкретном заказе, включая статус и список товаров.`
> `- **`POST /api/v1/tasks/{id}/complete/`**: Кастомный action для оператора, чтобы отметить задачу на размещение как выполненную.`

---

**6.2. Внешние интеграции:**
*Нужно ли будет интегрироваться с какими-либо внешними сервисами?*

> **Развернутый пример ответа:**
> `- **`Telegram Bot API`**: Для отправки критически важных уведомлений менеджерам (например, "Остаток товара X на исходе").`
> `- **`API DaData.ru`**: Для стандартизации и автодополнения адресов поставщиков.`
> `- **`API OpenAI`**: Для работы ИИ-ассистента.`

---

**6.3. Аутентификация API:**
*Как будет защищено ваше API?*

> **Развернутый пример ответа:**
> `Будет использоваться **Token-based аутентификация** на базе библиотеки `django-rest-framework-simplejwt`. Эндпоинт `/api/v1/token/` будет выдавать access и refresh токены в обмен на логин и пароль. Access токен будет жить 15 минут, refresh токен — 1 день.`

---

**6.4. Webhooks (входящие и исходящие):**
*Будет ли система принимать или отправлять webhooks?*

> **Развернутый пример ответа:**
> `- **Исходящий Webhook:** При изменении статуса `Заявки на пополнение` на "Утверждена", система будет отправлять POST-запрос на URL в N8n. Тело запроса будет содержать ID заявки и список товаров для заказа. Это позволит запустить воркфлоу по отправке заказа поставщику.`
> `- **Входящий Webhook:** Система будет предоставлять эндпоинт `/api/v1/webhooks/telegram/`, на который Telegram будет отправлять обновления, чтобы бот мог реагировать на команды от менеджеров.`

---

## Блок 7: Искусственный интеллект (AI)

*Этот блок определяет, как ИИ будет использоваться в проекте. Ответы лягут в основу архитектуры ИИ-компонентов, описанных в `specialized_systems_guide.md` и `arm_guide.md`.*

**7.1. Провайдеры LLM:**
*Какие провайдеры языковых моделей вы планируете использовать?*

> **Развернутый пример ответа:**
> `- **Основной провайдер:** OpenAI (модель `gpt-4.1-mini`) для задач, требующих высокого качества (например, ответы клиентам).`
> `- **Вспомогательный провайдер:** Alibaba Cloud (модель `qwen-turbo`) для внутренних, менее критичных задач (например, классификация тикетов) из-за более низкой стоимости.`
> `- **Локальные модели (SLM):** Не планируются на этапе MVP.`

---

**7.2. Сценарии использования ИИ:**
*Опишите 1-3 конкретные задачи, которые будет решать ИИ.*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Матрица Ценность vs. Сложность"**, чтобы выбрать наиболее эффективные сценарии для MVP.*
> 
> 1.  **Проведите мозговой штурм:** Выпишите все возможные идеи, где ИИ мог бы помочь.
>     - *Идея A:* Суммаризация длинных переписок с клиентами.
>     - *Идея B:* Автоматическое извлечение данных (сумма, дата) из PDF-счетов.
>     - *Идея C:* Прогнозирование оттока клиентов на основе их поведения.
>     - *Идея D:* Умный поиск по базе знаний по смыслу, а не по ключам.
> 2.  **Оцените каждую идею по двум шкалам (от 1 до 10):**
>     - **Ценность для бизнеса:** Насколько сильно это решает проблему пользователя или экономит деньги?
>     - **Сложность реализации:** Насколько трудно это реализовать (требуется ли дообучение моделей, сложная обработка данных)?
> 3.  **Нанесите идеи на матрицу и выберите те, что в правом верхнем квадранте (Высокая ценность, Низкая сложность):**
> 
> | Идея | Ценность | Сложность | Квадрант |
> |---|---|---|---|
> | A: Суммаризация | 8 | 3 | **Высокая ценность, Низкая сложность** ✅ |
> | B: Извлечение из PDF | 9 | 5 | **Высокая ценность, Низкая сложность** ✅ |
> | D: Умный поиск (RAG) | 8 | 4 | **Высокая ценность, Низкая сложность** ✅ |
> | C: Прогноз оттока | 10 | 9 | Низкая ценность, Высокая сложность ❌ |

> **Развернутый пример ответа:**
> `1. **ИИ-ассистент (Copilot) для службы поддержки:** В интерфейсе тикета будет кнопка "Предложить ответ". ИИ проанализирует текущий вопрос клиента и историю переписки, найдет релевантную информацию в базе знаний (RAG) и предложит черновик ответа, который сотрудник сможет отредактировать и отправить. **Ценность:** сокращение времени на ответ на 30%. **Сложность:** низкая.`
> `2. **Умный поиск по документации (RAG):** Внутренняя документация (инструкции, регламенты) будет векторизована и загружена в `pgvector`. Пользователи смогут задавать вопросы на естественном языке (например, "как оформить возврат бракованного товара?") и получать точный ответ со ссылкой на источник, вместо поиска по ключевым словам. **Ценность:** экономия времени сотрудников. **Сложность:** низкая.`
> `3. **Автоматическое извлечение данных из счетов:** При загрузке PDF-счета от поставщика, ИИ будет автоматически извлекать из него Номер счета, Дату, ИНН поставщика и Итоговую сумму, и предзаполнять поля в форме. **Ценность:** устранение ручного ввода данных. **Сложность:** средняя.`

---

**7.3. База знаний для RAG (Retrieval-Augmented Generation):**
*Если планируется ИИ-ассистент или умный поиск, на каких данных он должен основывать свои ответы?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Инвентаризация знаний"**.*
> 
> 1.  **Определите источники:** Где сейчас хранятся знания, которые нужны для ответов на вопросы?
>     - `База данных PostgreSQL`
>     - `Google Docs с инструкциями`
>     - `Confluence`
>     - `История переписки в Zendesk`
>     - `Публичная документация на сайте`
> 2.  **Оцените качество и доступность:**
>     - *Данные структурированы или нет?* (БД - да, Google Docs - нет)
>     - *Есть ли API для доступа?* (Zendesk - да, Google Docs - да)
>     - *Насколько данные актуальны?* (Нужно ли их чистить?)
> 3.  **Выберите источники для MVP:** Начните с 1-2 самых качественных и легкодоступных источников.

> **Развернутый пример ответа:**
> `База знаний для RAG будет состоять из следующих источников, которые будут периодически индексироваться и векторизоваться:`
> `- **Источник 1 (Структурированные данные):** Модели `Product` и `ProductCategory` из нашей основной БД. Это позволит ИИ отвечать на вопросы о характеристиках товаров.`
> `- **Источник 2 (Неструктурированные данные):** Все `.md` файлы из нашего репозитория `docs/internal_guides/`. Это позволит ИИ отвечать на вопросы по внутренним регламентам.`
> `- **Источник 3 (Внешние данные):** История решенных тикетов из Zendesk (будет выгружаться раз в сутки через API). Это позволит ИИ находить примеры решения похожих проблем.`

---

**7.4. ИИ-агенты и мультиагентные системы:**
*Планируется ли создание автономных агентов, которые могут выполнять задачи самостоятельно (например, по паттерну ReAct)?*

> **Развернутый пример ответа:**
> `Да, в рамках проекта планируется один **ИИ-агент "Аналитик отзывов"**.`
> `- **Триггер:** Запускается раз в сутки.`
> `- **Цель:** Мониторить отзывы о нашей компании на 2-3 внешних площадках.`
> `- **Инструменты (Tools):**`
>   `1. `web_search(query)`: Инструмент для поиска новых отзывов.`
>   `2. `analyze_sentiment(text)`: Инструмент для определения тональности отзыва (позитивный, негативный, нейтральный).`
>   `3. `create_ticket(title, description)`: Инструмент для создания задачи в нашей системе.`
> `- **Логика (ReAct):**`
>   `1. **Thought:** Мне нужно найти новые отзывы.`
>   `2. **Action:** `web_search("отзывы о компании iSmart Platform")`.
>   `3. **Observation:** Найден новый отзыв: "Все отлично, но доставка опоздала на день".`
>   `4. **Thought:** Отзыв содержит негативный элемент. Нужно проанализировать тональность и создать задачу.`
>   `5. **Action:** `analyze_sentiment("Все отлично, но доставка опоздала на день")`.
>   `6. **Observation:** `{"sentiment": "negative", "reason": "доставка опоздала"}`.
>   `7. **Thought:** Тональность негативная. Создаю тикет для отдела логистики.`
>   `8. **Action:** `create_ticket(title="Негативный отзыв о доставке", description="Клиент жалуется на опоздание доставки на 1 день. Ссылка на отзыв: ...")`.
>   `9. **Observation:** Тикет #12345 создан.`
>   `10. **Thought:** Задача выполнена.`

---

## Блок 8: Автоматизация (Automation)

*Этот блок описывает фоновые и периодические процессы, которые работают без прямого участия пользователя. Это основа для модуля "Автоматизация" в `arm_guide.md`.*

**8.1. Фоновые задачи:**
*Какие долгие операции нужно выполнять в фоне, чтобы не блокировать интерфейс пользователя?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм-чеклист "Кандидат в Celery"**.*
> 
> *Проверьте каждую операцию в системе по этому списку. Если хотя бы на один вопрос ответ "Да", операция — кандидат на вынос в фоновую задачу.*
> 
> - **Длительность:** Операция занимает больше 2-3 секунд? (Да/Нет)
> - **Внешний вызов:** Операция обращается к внешнему API, которое может медленно отвечать? (Да/Нет)
> - **Ресурсоёмкость:** Операция сильно нагружает процессор или память (обработка изображений, сложные вычисления)? (Да/Нет)
> - **Надёжность:** Нужно ли гарантировать выполнение операции, даже если пользователь закроет браузер (например, отправка заказа)? (Да/Нет)

> **Развернутый пример ответа:**
> `- **Генерация годового отчёта:** Операция может занимать несколько минут. **Кандидат.**`
> `- **Отправка email-уведомления:** Внешний вызов к SMTP-серверу, который может быть недоступен. **Кандидат.**`
> `- **Обработка загруженного аватара пользователя:** Создание нескольких версий разного размера (thumbnails). **Кандидат.**`
> `- **Создание нового комментария к задаче:** Операция быстрая и нересурсоёмкая. **Не кандидат.**`

---

**8.2. Задачи по расписанию:**
*Какие действия система должна выполнять автоматически по расписанию?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Мышление триггерами времени"**.*
> 
> *Подумайте о задачах, которые должны запускаться не по действию пользователя, а по времени.*
> 
> - **"Что нужно делать каждую ночь?"** → `Чистить старые данные, делать бэкапы, архивировать.`
> - **"Что нужно делать каждый час?"** → `Синхронизироваться с внешними системами, обновлять кэш.`
> - **"Что нужно делать раз в неделю/месяц?"** → `Отправлять сводные отчёты, пересчитывать рейтинги.`

> **Развернутый пример ответа:**
> `- **Каждый час (`0 * * * *`):** Запускать задачу синхронизации остатков с внешней WMS-системой.`
> `- **Каждый день в 02:00 (`0 2 * * *`):** Запускать задачу архивации заказов, которые находятся в статусе `Completed` или `Rejected` более 90 дней.`
> `- **Каждый понедельник в 09:00 (`0 9 * * 1`):** Запускать задачу, которая генерирует и отправляет на email всем менеджерам PDF-отчёт по ключевым метрикам за прошлую неделю.`
> `- **Каждые 5 минут (`*/5 * * * *`):** Запускать задачу проверки пороговых остатков товаров и создания черновиков заявок на пополнение.`

---

**8.3. Внешние воркфлоу (N8n, Make, etc.):**
*Планируете ли вы использовать инструменты вроде N8n для создания сложных сценариев автоматизации без кода?*

> **Развернутый пример ответа:**
> `Да, планируем активно использовать N8n для интеграционных и маркетинговых сценариев, чтобы не перегружать основной Django-проект.`
> `- **Сценарий 1: Онбординг нового клиента.**`
>   `- **Триггер:** Django отправляет webhook в N8n при регистрации нового пользователя.`
>   `- **Воркфлоу в N8n:**`
>     `1. Ждать 10 минут.`
>     `2. Отправить приветственное письмо через SendGrid.`
>     `3. Добавить пользователя в CRM (HubSpot).`
>     `4. Ждать 2 дня.`
>     `5. Отправить второе письмо с подборкой полезных статей из блога.`
> `- **Сценарий 2: Обработка заказа от поставщика.**`
>   `- **Триггер:** Django отправляет webhook в N8n при утверждении `Заявки на пополнение`.`
>   `- **Воркфлоу в N8n:**`
>     `1. Сформировать красивый PDF-файл с заказом.`
>     `2. Отправить этот PDF на email поставщика.`
>     `3. Создать напоминание в Google Calendar для менеджера, чтобы он проконтролировал оплату через 3 дня.`

---

## Блок 9: Контроль и Мониторинг (Control & Monitoring)

*Этот блок определяет, как вы будете отслеживать состояние системы, действия пользователей и ключевые показатели. Это основа для модуля "Контроль результата" в `arm_guide.md` и для методологии Log Driven Development.*

**9.1. Аудит действий:**
*Действия с какими сущностями необходимо логировать для последующего анализа или разбирательств?*

> **Развернутый пример ответа:**
> `Необходимо вести полный аудит (создание, изменение, удаление) для следующих сущностей с помощью библиотеки `django-auditlog`:`
> `- **`Заявка на пополнение`**: Чтобы отслеживать, кто и когда утверждал или отклонял заявки.`
> `- **`Товар`**: Чтобы видеть всю историю изменений цен, описаний и других атрибутов.`
> `- **`Права пользователей`**: Чтобы фиксировать любые изменения в правах доступа.`

---

**9.2. Дашборды и визуализация:**
*Какие ключевые показатели вы хотите видеть на дашборде в реальном времени?*

> #### 🤔 Как ответить на этот вопрос?
> *Используйте **алгоритм "Вопросы к бизнесу"**.*
> 
> *Представьте, что вы — руководитель. Какие 3-5 вопросов о состоянии дел вы задавали бы каждое утро? Ответы на них и есть ваши ключевые метрики для дашборда.*
> 
> - *"Сколько заказов мы получили сегодня/вчера?"* → **Метрика:** Количество новых заказов (по дням).
> - *"Что у нас с проблемными заказами?"* → **Метрика:** Распределение заказов по статусам.
> - *"Какие товары сейчас самые популярные?"* → **Метрика:** Топ-5 продаваемых товаров.
> - *"Мы успеваем отгружать вовремя?"* → **Метрика:** Среднее время от создания заказа до отгрузки.

> **Развернутый пример ответа:**
> `Основной дашборд для Менеджера склада должен содержать следующие виджеты:`
> `- **Линейный график "Динамика заказов за 7 дней"**: Показывает количество созданных и отгруженных заказов по дням.`
> `- **Круговая диаграмма "Статусы заказов"**: Показывает процентное соотношение заказов в статусах "Новый", "В сборке", "Готов к отгрузке", "Отгружен".`
> `- **Таблица "Топ-5 товаров в заказах (за месяц)"**: Список самых популярных товаров.`
> `- **KPI-карточка "Среднее время сборки"**: Одно число, показывающее среднее время от получения заказа до его полной сборки за сегодня.`
> `- **Таблица "Проблемные задачи"**: Список задач на размещение или сборку, которые просрочены.`

---

**9.3. Регламентные отчёты:**
*Какие отчёты система должна уметь генерировать в виде файлов (PDF, XLSX)?*

> **Развернутый пример ответа:**
> `- **Ежемесячный отчёт по продажам (XLSX):** Для бухгалтерии. Содержит список всех проданных товаров, их количество, сумму и НДС.`
> `- **Отчёт по оборачиваемости склада (PDF):** Для отдела логистики. Содержит ABC/XYZ-анализ товаров, выявляет неликвидные и самые ходовые позиции.`
> `- **Отчёт по работе операторов (PDF):** Для менеджера склада. Показывает количество выполненных операций (размещение, сборка) по каждому оператору за период.`

---

**9.4. Стандарт логирования:**
*Есть ли предпочтения по формату логов? Если нет, будет использован стандартный формат `[LEVEL][Class][Method][Step] Описание`.*

> **Развернутый пример ответа:**
> `Да, используем стандартный формат, но с добавлением `trace_id` для сквозной трассировки запросов между сервисами и Celery.`
> `**Формат:** `[LEVEL][trace_id][Class][Method][Step] Message``
> `**Пример:** `[INFO][a3f5b1...][OrderService][complete_order][Validation] Order #123 validation successful.``

---

## Блок 10: Инфраструктура и Деплой (Infrastructure & Deployment)

*Этот блок описывает, где и как будет работать ваше приложение.*

**10.1. Облачный провайдер:**
*Где будет размещаться проект?*

> **Развернутый пример ответа:**
> `Проект будет размещаться на **Yandex Cloud**. Будут использоваться сервисы: Yandex Compute Cloud (для виртуальных машин), Yandex Managed Service for PostgreSQL (для базы данных) и Yandex Object Storage (для хранения медиафайлов и бэкапов).`

---

**10.2. Операционная система на сервере:**
*Какую ОС вы предпочитаете для продакшен-сервера?*

> **Развернутый пример ответа:**
> `**Ubuntu 22.04 LTS** как наиболее стабильная и распространенная версия.`

---

**10.3. CI/CD (Непрерывная интеграция и доставка):**
*Планируете ли вы настраивать автоматическую сборку, тестирование и деплой при каждом пуше в репозиторий?*

> **Развернутый пример ответа:**
> `Да, через **GitHub Actions**. Воркфлоу будет состоять из следующих шагов:`
> `1. **Триггер:** Пуш в ветку `main`.`
> `2. **Шаг 1 (Test):** Запуск тестов (`pytest`) и линтеров (`flake8`, `black`).`
> `3. **Шаг 2 (Build):** Сборка Docker-образа (если будем использовать).`
> `4. **Шаг 3 (Deploy):** Подключение к серверу по SSH и выполнение скрипта `deploy.sh`, который скачивает последнюю версию кода, применяет миграции и перезапускает gunicorn.`

---

**10.4. Управление зависимостями:**
*Как вы предпочитаете управлять зависимостями проекта?*

> **Развернутый пример ответа:**
> `Использовать **Poetry**. Он обеспечивает детерминированную сборку, управление виртуальными окружениями и удобное разделение зависимостей на основные и для разработки. Файл `pyproject.toml` будет источником правды.`

---

**10.5. Среда разработки:**
*На какой ОС вы работаете? Какие IDE и терминалы используете? Это нужно для `USER_PROFILE.xml`.*

> **Развернутый пример ответа:**
> `- **ОС:** macOS Sonoma`
> `- **IDE:** VS Code с плагином Cursor`
> `- **Терминал:** iTerm2 с Oh My Zsh`

---

## Финальный блок: Маппинг ответов на артефакты проекта

*Эта таблица показывает, как ваши ответы на вопросы из этого опросника будут преобразованы в конкретные файлы и разделы проектной документации. Это обеспечивает полную прозрачность и помогает ИИ-ассистенту точно следовать вашим требованиям.*

| Блок опросника | Куда идут ответы (Артефакт проекта) | Назначение артефакта |
|---|---|---|
| **Блок 1: Видение и цели** | `docs/DEVELOPMENT_PLAN.md` (Раздел 1) | Главный документ, описывающий цели, объём и план работ. |
| | `.cursorrules` (Раздел "Цель проекта") | Глобальное правило для Cursor, чтобы он всегда помнил о цели. |
| **Блок 2: Бизнес-логика** | `docs/DEVELOPMENT_PLAN.md` (Раздел "Бизнес-сценарии") | Описание того, как пользователи будут взаимодействовать с системой. |
| | `[app_name]/models.py` | Определение моделей данных в Django. |
| | `[app_name]/workflows.py` (для `django-fsm`) | Описание жизненных циклов и статусов бизнес-процессов. |
| **Блок 3: Пользователи и роли** | `docs/USER_PROFILE.xml` | Персональный профиль разработчика для кастомизации ответов ИИ. |
| | `users/models.py` | Модель `User` с кастомными полями и ролями. |
| | `docs/DEVELOPMENT_PLAN.md` (Раздел "Роли и права") | Описание прав доступа для каждой роли. |
| **Блок 4: Архитектура** | `docs/DEVELOPMENT_PLAN.md` (Разделы 2, 3, 5) | Фиксация технического стека, архитектурных решений и ограничений. |
| | `.cursorrules` (Раздел "Технический стек") | Глобальное правило для Cursor, чтобы он использовал правильные технологии. |
| **Блок 5: Данные и Модели** | `[app_name]/models.py` | Прямое создание моделей Django с полями и связями. |
| | `[app_name]/admin.py` | Настройка отображения моделей в админ-панели Django. |
| **Блок 6: API и Интеграции** | `api/views.py`, `api/serializers.py` | Создание эндпоинтов и сериализаторов для REST API. |
| | `integrations/` (модуль) | Код для взаимодействия с внешними сервисами. |
| **Блок 7: Искусственный интеллект** | `agents/` (модуль) | Реализация ИИ-агентов и мультиагентных систем. |
| | `rag/` (модуль) | Код для систем Retrieval-Augmented Generation. |
| | `prompts/` (директория) | Хранилище промптов для различных ИИ-задач. |
| **Блок 8: Автоматизация** | `[app_name]/tasks.py` | Определение фоновых и периодических задач для Celery. |
| | `n8n_workflows/` (директория) | Хранение JSON-файлов с воркфлоу для N8n. |
| **Блок 9: Контроль и Мониторинг** | `core/logging_config.py` | Настройка конфигурации логирования. |
| | `dashboards/` (модуль) | Код для создания дашбордов и отчётов. |
| | `[app_name]/models.py` (интеграция с `django-auditlog`) | Подключение аудита к нужным моделям. |
| **Блок 10: Инфраструктура** | `.github/workflows/` | Файлы для настройки CI/CD. |
| | `requirements.txt` или `pyproject.toml` | Список зависимостей проекта. |
| | `README.md` | Инструкции по развёртыванию и запуску проекта. |

---

После заполнения этого опросника у вас будет вся необходимая информация для старта проекта. Вы можете передать этот документ (или ответы из него) LLM-ассистенту в Cursor, и он сможет сгенерировать на его основе полную и структурированную проектную документацию, а затем и сам код.
